// learning about control flow

// all the relational operators are same
// execpt these === and !==

if(5=="5")
{
    console.log(`5 is == to "5" `);
}
if(5==="5")
{
    console.log(`5 === "5`);
}
else{
    console.log(`5 is not ==="5"`);
}

// also

if(5!=="5")
{
    console.log("it is true that 5!=='5'");
}
 // && and || bhi same hi hai

 // switch bhi pura same hai c++ ki tarah

 let val=5;

 switch(val)
 {
    case 1:console.log( 1);
    break;
    case 2: console.log( 2);
    break;
    default: console.log(3);;
 }

 // alt + sht + down = copy paste data immediately

 // falsy values
 // false 0, -0, "",null ,undefined, null

 // truthy values cannot be sured off

 // inside string agar size one bhi hai to true
 // khali object, array and function bhi true values dete hai
